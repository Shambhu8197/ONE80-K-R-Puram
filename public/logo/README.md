# ONE80° EVENT CENTER logo

The official, unmodified logo asset is `one80-logo.png.avif`.

The shared `Logo` component points to `/logo/one80-logo.png.avif`. If the approved logo arrives in a different filename or format, update that `src` only—do not recreate or alter the mark.
